name: Build Android for store

on:
  workflow_dispatch: {}

env:
  node-version: '20.x'  # Set Node.js version globally

jobs:
  buildAndroid:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Cache Unity Library
        uses: actions/cache@v2
        with:
          path: path/to/your/project/Library
          key: Library-UnityAndroid
          restore-keys: |
            Library-Unity-
            Library-

      - name: Activate Unity License
        run: |
          mkdir -p $HOME/.local/share/unity3d/Unity/
          echo "$UNITY_LICENSE" > $HOME/.local/share/unity3d/Unity/Unity_lic.ulf

      - name: Build Android APK
        uses: game-ci/unity-builder@v2
        with:
          targetPlatform: Android 
          androidAppBundle: 'true'
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v2
        with:
          name: Build-Android
          path: path/to/your/project/build/Android
